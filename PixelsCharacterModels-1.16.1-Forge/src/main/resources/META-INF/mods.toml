
modLoader="javafml" #mandatory

loaderVersion="[31,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.

issueTrackerURL="http://my.issue.tracker/" #optional

[[mods]] #mandatory
modId="pcm" #mandatory

version="0.5" #mandatory

displayName="Pixel's Character Models" #mandatory

updateJSONURL="https://raw.githubusercontent.com/PixelDoted/MyUpdateTrackers/master/jsonTracker/PCM-1.15.2" #optional

displayURL="curseforge.com/minecraft/mc-mods/pixels-character-models" #optional

logoFile="icon.png" #icon.png #optional

credits="PixelDots, Edoren for skinchanger" #optional

authors="PixelDots" #optional

description='''
	a way to customize your player
'''

[[dependencies.PCM]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[31,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.PCM]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.15.2]"
    ordering="NONE"
    side="BOTH"
[[dependencies.PCM]]
	modID="obfuscate"
	mandatory=true
	versionRange="[1.15.2]"
	ordering="NONE"
	side="BOTH"