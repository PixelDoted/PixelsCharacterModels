package me.PixelDots.PixelsCharacterModels.server.model;

public class ServerModelsPacket {

    /*public ServerModelsPacket(PacketBuffer buf) {
    }

    public void encode(PacketBuffer buf) {
    }
    
    public static void handle(ServerModelsPacket packet, Supplier<NetworkEvent.Context> ctx) {
    	ctx.get().enqueueWork(() -> {
            // Work that needs to be threadsafe (most work)
            ServerPlayerEntity sender = ctx.get().getSender(); // the client that sent this packet
            GlobalModelManager.Model.sendAllToPlayer(sender, false);
            // do stuff
        });
        ctx.get().setPacketHandled(true);
    }*/
    
}